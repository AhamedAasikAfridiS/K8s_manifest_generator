# Docker

The primary orchestration file is at the repository root: `docker-compose.yml`.

Service-specific Dockerfiles:

- `authservice/Dockerfile`
- `ai-agent-service/Dockerfile`
- `frontend/Dockerfile`

Frontend nginx config: `frontend/docker/nginx.conf`
